# gitignore .DS_Store

**.DS_Store** is a file in OSX for storing some metadata of a folder such as the position of an icon or the choice of a background image.

This file is created and maintained by the *Finder* in every folder.

Add `.DS_Store` into the `.gitignore` could ignore all of them.